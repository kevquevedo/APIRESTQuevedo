import { ConversorNombrePipe } from './conversor-nombre.pipe';

describe('ConversorNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new ConversorNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
